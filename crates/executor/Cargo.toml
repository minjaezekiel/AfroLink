[package]
name = "afrolink-executor"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Deterministic block execution: applies transactions to state and commits an app hash."

[dependencies]
afrolink-primitives.workspace = true
afrolink-crypto.workspace = true
afrolink-state.workspace = true
afrolink-types.workspace = true
afrolink-bank.workspace = true
afrolink-alias.workspace = true
afrolink-consensus.workspace = true
afrolink-staking.workspace = true
afrolink-gov.workspace = true
thiserror.workspace = true

# Only the tests need to *construct* a payment reference. Execution merely asks
# whether one is present, through `Account::requires_reference`, so the enforcing
# code has no dependency on the payments crate at all.
[dev-dependencies]
afrolink-pay.workspace = true

[lints]
workspace = true
